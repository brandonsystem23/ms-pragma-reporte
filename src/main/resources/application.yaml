server:
  port: 8085

spring:
  application:
    name: reporte-service

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/local}

security:
  jwt:
    secret: ${JWT_SECRET:PragmaJwtSecret_2026_fK2mQ9xL7vN4pR6tY3wZ1aB5cD8eF0gH}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    org.springframework.security: INFO
